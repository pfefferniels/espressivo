node_modules/
dist/
coverage/
package-lock.json

# Agent worktrees: nested checkouts of this repo, formatted on their own branch.
.claude/worktrees/

# Java-generated ground truth + MEI inputs. Immutable.
tests/integration/fixtures/

# Vendored third-party MPM/MSM from the MPM format's own sample corpus (BSD-2 + CC BY 4.0).
# Two reasons, both hard: Prettier infers no parser for .mpm/.msm and errors out on them, the
# same reason the line above exists; and these files are byte-faithful copies whose UTF-8 BOM
# and single-quoted attributes are the point — a reformat would destroy what they pin. See
# tests/comparison/fixtures/PROVENANCE.md.
tests/comparison/fixtures/

# The target-architecture ruling. Its RULEs are cited by line anchor from PARITY.md and from
# `src/`, so a reflow would silently invalidate every one of them while leaving the build green.
# Verified: without this entry `prettier --check ARCHITECTURE.md` reports the file as needing
# reformatting.
/ARCHITECTURE.md
