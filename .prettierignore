node_modules/
dist/
coverage/
package-lock.json

# Java-generated ground truth + MEI inputs. Immutable (charter invariant 2).
tests/integration/fixtures/

# Vendored third-party MPM/MSM from the MPM format's own sample corpus (BSD-2 + CC BY 4.0).
# Two reasons, both hard: Prettier infers no parser for .mpm/.msm and errors out on them, the
# same reason the line above exists; and these files are byte-faithful copies whose UTF-8 BOM
# and single-quoted attributes are the point — a reformat would destroy what they pin. See
# tests/comparison/fixtures/PROVENANCE.md.
tests/comparison/fixtures/

# Swarm coordination journal — hand-formatted, reflowing it would churn the log.
refactor/

# The MPM v3 ornamentation programme's journal, charter, design doc, research reports and
# probe tools. Same rationale as refactor/ above, and one more: LOG.md is append-only, so a
# reformat would rewrite ~400 lines inside already-committed waves' entries. The two probe
# tools are byte gates — they are copies of the refactor swarm's and must stay comparable.
ornamentation/

# The expression-transform campaign's charter, design doc, survey and journal. Same rationale
# again, and measured: reflowing DESIGN.md alone rewrites ~1000 lines of already-committed
# prose, and LOG.md is append-only. These documents were already outside Prettier's style
# before this entry existed; the entry records that, rather than changing it.
# Root-anchored: unanchored, it would also exempt src/expression/ and tests/expression/, which
# are ordinary source and must stay under the formatter.
/expression/
