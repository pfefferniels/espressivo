node_modules/
dist/
coverage/
package-lock.json

# Java-generated ground truth + MEI inputs. Immutable (charter invariant 2).
tests/integration/fixtures/

# Vendored third-party MPM/MSM from the MPM format's own sample corpus (BSD-2 + CC BY 4.0).
# Two reasons, both hard: Prettier infers no parser for .mpm/.msm and errors out on them, the
# same reason the line above exists; and these files are byte-faithful copies whose UTF-8 BOM
# and single-quoted attributes are the point — a reformat would destroy what they pin. See
# tests/comparison/fixtures/PROVENANCE.md.
tests/comparison/fixtures/

# The completed campaigns' journals, charters, design docs, research reports and probe tools
# (refactor swarm, MPM v3 ornamentation, expression transform). Hand-formatted and mostly
# append-only: reflowing them would churn logs that are already committed — DESIGN.md alone
# rewrites ~1000 lines — and the probe tools under ornamentation/tools/ are byte gates, copies
# of the refactor swarm's that must stay comparable.
# This one entry replaces three separate root-level entries. The old `/expression/` entry
# needed a root anchor to avoid also catching src/expression/ and tests/expression/, which are
# ordinary source; nesting the records under docs/ makes that ambiguity impossible to hit.
docs/history/

# The live comparison campaign's record (moves to docs/history/comparison/ at merge, W5).
comparison/

# The target-architecture ruling. Exempt for a stronger reason than the journals: its RULEs are
# cited by ~85 `ARCHITECTURE.md:NNN` line anchors from PARITY.md and the campaign records, so a
# reflow would silently invalidate every one of them while leaving the build green. The file was
# exempt implicitly while it sat inside refactor/; promoting it to the repo root is what makes
# the exemption something that has to be written down. Verified: without this entry
# `prettier --check ARCHITECTURE.md` reports the file as needing reformatting.
/ARCHITECTURE.md
