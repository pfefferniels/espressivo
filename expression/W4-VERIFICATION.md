# W4 adversarial verification — full findings (2 lenses, 2026-08-09)


## Agent a14aee779764b53fa

**Verdict:** PASS-WITH-FIXES. The engineering is conformant on every load-bearing point Lens A asked about. D-I's type→dimension table is transcribed row for row in src/expression/selection.ts:54-62 with no drift (including `movement`→`pedalShape` per A9, and the distribution family resolved by enclosing map rather than by element name, with `imprecisionMap.tuning` correctly yielding no dimension and therefore an `unmappable` offender rather than a silent identity); `DISTRIBUTION_ELEMENTS` covers exactly the six MPM distribution element names, so the `distribution.*` wildcard is complete; `<measurement>` is unmappable and the enclosing `<distribution.list>` is the atomic selection, as §7.13 requires. `ids: []` is the identity by an explicit branch (src/api/expression.ts:441-451) and, because every resolved id yields at least one dimension, an empty spared set can only mean an empty selection — the prototype's flatten-everything is unreachable. The one-throw-lists-all contract holds with both kinds tagged, and the no-partial-run guarantee is structural: resolveSelection and requireResolvedSelection both precede `transform` (src/api/expression.ts:403-411) and selection.ts contains no write path. `attenuation ∈ (0,1]` is enforced pre-parse with 1 admitted as the identity and documented as such in three places. Gesture wiring is genuinely the same engine, not a parallel path: an executed scratch reproduction showed `spotlightMpm(mpm,{ids:['pt'],attenuation:0.25})` byte-identical in `.mpm` and deep-equal in `.report` to `exaggerateMpm(mpm,{factors:<derived>,scope:'gesture'})`, with 17 writes on both sides. `weightedFactors` implements D-H's lerp in the prototype's own spelling, defaults a missing weight to 1, returns all fifteen keys, and all eight numeric values match PerformService.getDefaultWeights exactly, with the articulation collapse resolved to the lower (0.2) and its reason stated. RULE conformance is clean: SelectionNotFoundError extends MeicoError in errors.ts house style and is exported from src/index.ts; SpotlightResult is plain data with a JSON and structuredClone round-trip pinned; no field can be undefined. PARITY §7's four named divergences each check out against the Java source (articulation declared but never applied; `Shader.bringOut(perf, keepIds, 0.1)` hardcoded at PerformService.java:99; Shader.java:46-53's bare `continue` plus silent skip of unmapped types; Isolation.contextualize comparing a date to a volume at Isolation.java:436 and using one as a date at :443), and nothing in src/expression is imported by the conversion or rendering paths. The fixes are documentation, not mechanism: one MAJOR — the PROTOTYPE_WEIGHTS correspondence table asserts three prototype behaviours that ModifyService.java refutes, and those false assertions are the only justification for three non-neutral weights on dimensions the prototype had no lever for, which also makes the module's own 'the honest value is the neutral one' rule read as inconsistently applied — plus six MINORs (two README qualifiers dropped from R5a/R5b, a tautological no-partial-run pin, an 'unconditionally/every render' overstatement, three stale 'LANDING IN W4' markers, a contradicted 'all optional' lead-in, and a concurrency hazard in the worktree itself that requires one clean re-run before certification).

### [MAJOR] PROTOTYPE_WEIGHTS' documented correspondence misdescribes the prototype in three of its fifteen rows, and the module's own honesty rule is applied inconsistently as a result

- **Claim:** The eight numeric values in PROTOTYPE_WEIGHTS match getDefaultWeights() exactly, and the articulation collapse (0.2, the lower, with its reason) is correct. But three rows of the correspondence table in src/expression/weights.ts state facts about the prototype that the prototype's source refutes, and each of those false facts is the sole justification for shipping a non-neutral weight on a dimension the prototype had no lever for. Since this module exists to make the prototype 'reproducible and inspectable' (weights.ts:17-19, 76-78), a correspondence that misdescribes the source defeats its purpose.
- **Evidence:** (1) src/expression/weights.ts:53 — `| dynamics | 1.1 | dynamics, dynamicsShape | likewise |`, where 'likewise' back-references 'A9 split level contrast from curve shape after the vector was tuned; the shape half inherits'. Refuted: ModifyService.exaggerateDynamics (/Users/nielspfeffer/Projects/mpm-renderer/core/src/main/java/meicotools/core/ModifyService.java:227-245) writes only `@volume` and `@transition.to` — `@curvature`/`@protraction` are never read or written anywhere in the prototype (`grep -n 'curvature|protraction' ModifyService.java` returns nothing). There was no dynamics curve-shape lever to split. The parallel claim IS true for tempo: exaggerateTempo (ModifyService.java:248-276) does rescale `meanTempoAt` at :265-272, so `tempoShape` genuinely inherits. (2) src/expression/weights.ts:56 — 'one prototype field drove the frame and its spacing exponent together'. Refuted: exaggerateTemporalSpread (ModifyService.java:291-317) reads `ts.getFrameLength()` and calls `ts.setFrameLength(...)` and nothing else; `@intensity`, which is this port's entire `ornamentSpacing` dimension (DESIGN §7.10), is never touched. DESIGN's own D-E describes the prototype's behaviour as a collection-geomean on frameLength only. (3) src/expression/weights.ts:57 — `| dynamicsGradient | 0.3 | ornamentDynamics |`. The prototype field of that name feeds exaggerateDynamicsGradient (ModifyService.java:320-327), whose body is `el.getAttribute("scale")` … `new Attribute("scale", …)` over the ornamentation MAP — i.e. it scales `ornament@scale`, the attribute DESIGN §7.16 / RESOLVED-6 explicitly EXCLUDES. The gradient endpoints `dynamicsGradient@transition.from`/`.to` that `ornamentDynamics` covers were never scaled by the prototype. The correspondence is by element name, not by lever. Consequence: weights.ts:70-73 says '**The five dimensions the prototype had no field for** — asynchrony, the three imprecision domains, and pedalShape — are 1.0 … the prototype could not express them, so the honest value is the neutral one'. On the evidence above it is eight dimensions, and three of them (dynamicsShape 1.1, ornamentSpacing 1.5, ornamentDynamics 0.3) ship non-neutral weights under a rationale the source contradicts.
- **Fix:** Keep the numbers — DESIGN §3's 'Preset mapping (D-H)' paragraph owns them and I am not re-litigating it — but replace the three notes with what the source actually shows, and re-label the values as a design choice rather than an inherited prototype fact: e.g. `dynamicsShape` note → 'the prototype had NO dynamics curve-shape lever; the split dimension inherits `dynamics`' weight by decision (a preset with a hole is worse than one that over-applies), not by inheritance'; `ornamentSpacing` note → 'the prototype scaled `@frameLength` only (ModifyService.java:291-317); `@intensity` is unweighted there and inherits by the same decision'; `ornamentDynamics` note → 'the prototype''s `dynamicsGradient` field in fact scaled `ornament@scale` (ModifyService.java:320-327), which RESOLVED-6 excludes here; the 0.3 is carried onto the gradient endpoints by name and intent'. Then either extend the 'no field for' list at weights.ts:70-73 to name dynamicsShape/ornamentSpacing/ornamentDynamics, or state in one sentence why the honesty rule stops at the A9 splits. The same three sentences need correcting in DESIGN §3's preset-mapping paragraph (expression/DESIGN.md:298-310) and in PARITY.md §7.

### [MINOR] README's R5a/R5b restatements drop the two qualifiers that make them true

- **Claim:** The README's 'What is guaranteed' bullets are the public-facing statement of §2's split R5, and both drop a qualifier that the facade JSDoc and §2 carry deliberately. R5b becomes self-contradictory as written; R5a invites exactly the wrong diff.
- **Evidence:** README.md:310 — 'Performing the result against the same MSM yields the same notes — same ids, dates, durations and pitches'. §2's R5a/R5b block (expression/DESIGN.md:186-190) and the facade JSDoc (src/api/expression.ts:211-212) both say **symbolic** dates and durations, and the JSDoc adds the clause README omits: 'only milliseconds, velocities and control changes move'. Read plainly, 'same dates and durations' says nothing moves, which is the opposite of what exaggeration does. README.md:307 — 'The result has the same skeleton as the input'. The facade says 'the same skeleton as the input's **canonical form**' (src/api/expression.ts:205-207), and A2/§1.1 exist because parse+serialize re-emits `xmlns` on every namespaced element — src/api/expression.ts:100-103 records the measured inflation, 2444 → 4011 bytes on a real fixture, and every serialized document in this suite shows `<performance xmlns="http://www.cemfi.de/mpm/ns/1.0">` on each element. README's `canonicalMpm` table row (README.md:166, 'MPM text — parsed and re-serialized') never tells a reader that it is the baseline an identity or skeleton claim must be compared against, so a reader diffing `exaggerateMpm(x).mpm` against `x` sees attributes added on every element and concludes R5a is false.
- **Fix:** README.md:310 — insert 'symbolic' before 'dates' and append the JSDoc's clause: '…the same notes — same ids, symbolic dates, durations and pitches; only milliseconds, velocities and control changes move.' README.md:307 — 'the same skeleton as `canonicalMpm(input)`', and add half a sentence to the canonicalMpm table row or the section prose saying it is the baseline any byte comparison is made against (parsing re-emits namespace declarations, so the input's own bytes are not the reference).

### [MINOR] The 'never a partial run' pin in spotlight.test.ts is a tautology and cannot fail

- **Claim:** D-I/A8's strongest safety claim — 'There is NEVER a partial run: the document is untouched when it throws' — is pinned by an assertion that is `Object.is(s, s)` on an immutable module-level string. It would pass unchanged if spotlightMpm wrote the whole document before throwing. The engine is in fact correct here; the test simply provides no evidence for it.
- **Evidence:** tests/api/spotlight.test.ts:290-294: `const before = ALL_MAPS; expect(() => spotlight(ALL_MAPS, ['ghost'])).toThrow(SelectionNotFoundError); expect(ALL_MAPS).toBe(before);` — ALL_MAPS is a module-level `const` string (tests/api/spotlight.test.ts:59-70), so the final assertion is true by construction. The behaviour itself is sound and structurally guaranteed: src/api/expression.ts:403-411 sequences parseRoot → resolveSelection → requireResolvedSelection → transform, and src/expression/selection.ts only reads (`getChildElements`, `attribute`) with no write path. Executed de-vacuized reproduction (scratch, since deleted): on `all_maps.mpm` with an `xml:id="pt"` grafted onto `<tempo date="2880.0">`, `spotlightMpm(mpm, {ids:['pt','ghost'], attenuation:0.25})` throws while `spotlightMpm(mpm, {ids:['pt'], attenuation:0.25}).report.totalWrites === 17` — so the refused run demonstrably had 17 writes to make.
- **Fix:** Replace the tautology with the observable pair: assert the throw for `['pt','ghost']`, then assert that `['pt']` alone reports `totalWrites > 0`, so the test states 'the run that was refused was one that would have written'. Optionally also assert `spotlightMpm(mpm,{ids:['pt']}).mpm !== canonicalMpm(mpm)` to pin that the refusal suppressed a real edit.

### [MINOR] 'getDefaultWeights() applied unconditionally / to every render' overstates the prototype

- **Claim:** Both the weights module's opening rationale and PARITY §7 say the tuned vector was applied to every render. It was applied only on the exaggeration path; a sketchiness-only render built an Exaggerate and never weighted it.
- **Evidence:** src/expression/weights.ts:21 — 'The prototype applied `getDefaultWeights()` unconditionally, so every render it produced was exaggerated through one person's taste'. PARITY.md:987 — 'the eight tuned weights `getDefaultWeights()` applied to **every** render invisibly'. In /Users/nielspfeffer/Projects/mpm-renderer/core/src/main/java/meicotools/core/PerformService.java:92-95 the call sits inside `if (exaggerate != null) { params.exaggerate = new Exaggerate(exaggerate); params.exaggerate.applyWeights(getDefaultWeights()); }`, and PerformService.java:104-115 shows the sketchiness path constructing `params.exaggerate = new Exaggerate(1.0)` when `exaggerate == null`, with no applyWeights call.
- **Fix:** Narrow both sentences to 'every render that asked for exaggeration' / 'applied invisibly to every exaggerated render'. The rhetorical point (no way to see the taste or turn it off) survives intact.

### [MINOR] DESIGN still describes W4's shipped deliverables in the future tense

- **Claim:** The spec the W5 audit will read announces spotlightMpm and PROTOTYPE_WEIGHTS as forthcoming, although this wave lands both.
- **Evidence:** expression/DESIGN.md:360 — 'SelectionNotFoundError — thrown by spotlightMpm … (W4 amendment: landing in W4 alongside spotlightMpm itself)'. expression/DESIGN.md:580 — '**LANDING IN W4** as `spotlightMpm` (W4 amendment)'. expression/DESIGN.md:581 — '**LANDING IN W4** as `PROTOTYPE_WEIGHTS` — exported data, not a static method, per D-H (W4 amendment)'. All three are shipped in this tree (src/api/expression.ts:400, :557; src/index.ts exports both).
- **Fix:** Change the three markers to past tense ('landed in W4') or drop the wave marker and simply describe the shipped surface, keeping the '(W4 amendment)' provenance tag.

### [MINOR] README's 'Options, all optional:' now heads two option bags with required fields

- **Claim:** The two new bullets were appended under a lead-in that contradicts them; each bullet has to correct the heading in its own first clause.
- **Evidence:** README.md:171 reads 'Options, all optional:'; README.md:182 adds '- **`ExaggerateOptions`** — `factors` (required), …' and README.md:189 adds '- **`SpotlightOptions`** — `ids` and `attenuation` (both required), …'. (A blank line at README.md:181 also splits what renders as one list, making it loose.)
- **Fix:** Reword the lead-in to 'Options:' and let each bullet say which fields are required, or move the two new bullets under their own lead-in ('Expression options — `factors`, `ids` and `attenuation` are required, the rest optional:').

### [MINOR] The worktree was mutated by a concurrent agent during this verification, so any green recorded against it is not reproducible without a re-run

- **Claim:** This is a process finding, not a code defect, but it is material to how the verdict should be used: three of my early observations were artifacts of a moving tree, and one of the transient states was a live D-I violation. The final content is byte-identical to my snapshot and the suite is green, but the tree must be quiesced before certification.
- **Evidence:** src/expression/selection.ts (mtime 10:00:33) briefly carried an extra row in TYPE_DIMENSIONS — `['styleDef', ['tempo', 'rubato']]` — under which a `<styleDef>` id would have resolved and spared `tempo`+`rubato`, flatly against D-I's table ('`style`, and any other type | none ⇒ SelectionNotFoundError'); it was gone again by mtime 10:01:02 and an executed probe at 10:01:26 confirmed the shipped message lists only the eight D-I types. src/api/expression.ts was rewritten at 09:59:55 during a full-suite run started at 09:59:37, producing a spurious failure at tests/api/expression-facade.test.ts:348 (PerformanceNotFoundError where InvalidOptionError was expected) that does not reproduce on the settled file. tests/expression/weights.test.ts's DOCUMENT constant changed from `milliseconds.offset="30.0"` to `"20.0"` mid-review, producing another spurious failure. Settled state, verified: `diff` of every W4 file against my 09:56 snapshot is empty; `npx tsc --noEmit` clean; `npx eslint` on all nine W4 files clean; `npx vitest run` = 89 files / 3958 tests passed with no source or test file touched during the run.
- **Fix:** Before certifying W4, confirm no sibling agent holds the worktree (compare `find src tests -newermt <start>` before and after), then re-run `npm run verify` once end to end and record that run as the evidence.


## Agent a180c342bb9dad9f7

**Verdict:** PASS-WITH-FIXES. The ~119 new tests are substantially honest and, on the claims Lens B asks about, they hold up under adversarial mutation. (1) The foreground pin is genuine byte/string equality on serialized attribute text, not `toEqual` on parsed values (`tests/api/spotlight.test.ts:165-170`) — though it covers 3 of the element's 5 attributes while its title claims all. (2) The background-attenuates claim is pinned twice — written values with a geometric-mean invariant (`spotlight.test.ts:172-181`) and rendered direction (`tests/integration/expression-spotlight.test.ts`), the latter with a real anti-vacuity guard (`assertShrinks` asserts the unattenuated reading is non-zero before requiring strict monotone decrease across 1/0.75/0.5/0.25, plus a sanity test that the untouched document performs both gestures). (3) The error surface is pinned by class and by message, and the mixed unresolved+unmappable case is present in ONE throw with an offender-count regex (`/3 of the 4 selected ids/`) that pins list completeness; attenuation rejections pin the class but only one row pins the message. (5) The A7 piecewise-constant inert pin is present and non-vacuous (`state === 'inert'`, `writes === 0`, `sitesInert > 0`, and a `constant-instruction` note). (6) No expects-in-conditionals and no vacuous passes beyond one `Number(null)` window. (7) No scratch files remain, and the A14 dimension table (`tests/integration/expression-transform.test.ts:717`) was not extended and keeps its completeness guard. Five targeted mutations of the load-bearing claims were all caught, several broadly: spared-dimensions-not-spared → 14 failures; empty-selection-flattens (the prototype's worst defect) → 2; first-offender-only → 1; the prototype's silent `continue` on unresolved ids → 6; distribution-dimension-from-name-not-map → 5. What blocks a clean PASS is one MAJOR hole in a *different* direction from the ones the lens anticipated: W4's second deliverable, the public `weightedFactors`, is tested only through its internal module, and the facade wrapper that supplies its entire documented contract can be deleted with the full 3958-test suite still green. Two MINOR closure gaps compound it — D-I's type table and the facade's spotlight table both admit silent additions/omissions (a bogus 9th selectable type passes the whole suite; four dimensions never reach `spotlightMpm`, though I verified by scratch reproduction that both uncovered paths behave correctly, so these are coverage rather than live defects). All fixes are additive test work; nothing found requires a change to W4's shipped behaviour.

### [MAJOR] The public `weightedFactors` facade is untested — its only reason to exist (error typing) is dead under test

- **Claim:** All 30 weights assertions import the **internal** module `src/expression/weights.js`, which throws plain `Error`. The public export at `src/api/expression.ts:538` is a try/catch wrapper whose entire purpose is to translate that into `InvalidOptionError` (its own `@throws {InvalidOptionError}` doc, RULE E2). Nothing anywhere imports `weightedFactors` or `PROTOTYPE_WEIGHTS` from `src/api/index.js` or `src/index.js`, so W4's whole D-H public surface is unexercised: a JS caller passing `{tempoShapes: 0.5}` is documented to get a `MeicoError` subclass and no test proves it does.
- **Evidence:** `grep -rn 'weightedFactors\|PROTOTYPE_WEIGHTS' tests/` returns hits only in `tests/expression/weights.test.ts`, whose imports (lines 16-21) are `from '../../src/expression/weights.js'`. Executed mutation: replacing the wrapper body at `src/api/expression.ts:538-544` with a bare `return computeWeightedFactors(s, weights);` (deleting the try/catch entirely) → `npx vitest run` reports **3958/3958 passing, 0 failures**. Same for the barrel: stripping `spotlightMpm`, `weightedFactors`, `PROTOTYPE_WEIGHTS`, `SelectionNotFoundError` and the four new type exports from `src/index.ts` → full suite green and `tsc --noEmit` clean. (The `src/index.ts` half is a pre-existing convention — only `tests/HelperShim.test.ts` imports the root barrel — but the `src/api/expression.ts` wrapper is new W4 behaviour that other W4 tests *do* reach via `src/api/index.js`.)
- **Fix:** Add a short block to `tests/expression/weights.test.ts` (or `tests/api/expression-facade.test.ts`) importing `weightedFactors`/`PROTOTYPE_WEIGHTS` from `../../src/api/index.js`: assert each of the four rejection rows throws `InvalidOptionError` *and* `MeicoError`, that the message still names the offending dimension (`/rubatoo/`, `/rubato/`), that `cause` is preserved, and that `PROTOTYPE_WEIGHTS` reached through the facade is the same object as the internal one. One `exaggerateMpm(mpm, { factors: weightedFactors(1.6, PROTOTYPE_WEIGHTS) })` round trip would also pin the composition the docstring advertises.

### [MINOR] D-I's type table has no closure guard — a spurious selectable type is invisible to the entire suite

- **Claim:** `selection.ts:17-18` claims "`tests/expression/selection.test.ts` cross-checks the other eight against `REGISTRY_ROWS`, so the two can only diverge deliberately." That is refuted for one direction: the cross-check block (`selection.test.ts:174-191`) iterates a **hardcoded** 7-row list, and the completeness guard at `selection.test.ts:212-221` only guards the *dimension* axis (every dimension is reachable). Nothing guards the *type* axis, and `TYPE_DIMENSIONS` (`selection.ts:50-62`) is module-private so no test can enumerate it. An accidental extra row silently widens the selection vocabulary — i.e. makes an unselectable element spotlight-able and spare dimensions it does not govern.
- **Evidence:** Executed mutation: adding `['styleDef', ['tempo', 'rubato']],` after `['movement', ['pedalShape']]` in `src/expression/selection.ts:61` → `npx vitest run` reports **3958/3958 passing**. With that mutant, `spotlightMpm(mpm, {ids: ['someStyleDefId'], attenuation: 0.1})` succeeds and spares tempo+rubato instead of raising `SelectionNotFoundError`, and no test notices.
- **Fix:** Export `TYPE_DIMENSIONS` (or a derived `SELECTABLE_TYPES` array) from `selection.ts` and add the missing half of the guard: `expect([...SELECTABLE_TYPES].sort()).toEqual([...the 8 D-I rows].sort())`, plus assert each non-`ornament` row equals `dimensionsWrittenAt(element)` by iterating the exported table rather than the hand-typed list.

### [MINOR] Four dimensions — all three ornament rows and `imprecisionDuration` — never go through `spotlightMpm`, with no guard to say so

- **Claim:** The facade table at `tests/api/spotlight.test.ts:108-124` has 8 rows covering 11 of the 15 dimensions. `ornamentSpread`, `ornamentSpacing`, `ornamentDynamics` and `imprecisionDuration` are exercised only by `resolveSelection` unit tests on hand-built XML; they never reach `spotlightFactors` → applier through the public entry point. The header comment admits the `<ornament>` gap but not the `imprecisionDuration` one, and there is no `expect(covered).toEqual(EXPRESSION_DIMENSIONS)`-style guard, so a fifth uncovered dimension would appear silently. `<ornament>` is the highest-risk row: it is the *only* entry where D-I's selection vocabulary and the registry's write vocabulary deliberately disagree (the dimensions write into `<temporalSpread>`/`<dynamicsGradient>` children of a *different* element).
- **Evidence:** Executed scratch (`tests/expression/scratch-lensb.test.ts`, since deleted): `spotlightMpm(ornamentation.mpm, {ids:['orn1'], attenuation:0.5})` → `spared = ['ornamentSpread','ornamentSpacing','ornamentDynamics']`, `resolvedIds = [{id:'orn1',element:'ornament',dimensions:[...3]}]`, `totalWrites = 0`, output byte-identical to `canonicalMpm`. A hand-built `imprecisionMap.toneduration` doc → `spared = ['imprecisionDuration']`, `totalWrites = 2` (the sibling timing distribution's `limit.lower/upper` halved from ±5.0 to ±2.5), so both paths behave correctly — this is coverage, not a live defect. The fixture needs no graft: `tests/integration/fixtures/all-maps-reference/ornamentation.mpm` already carries `xml:id="orn1"…"orn4"`. No fixture in the corpus has an `imprecisionMap.toneduration` (`grep -rl 'imprecisionMap.toneduration' tests/integration/fixtures/` → empty).
- **Fix:** Add an `ornamentation.mpm` row to the facade table (id `orn1`, spares the three ornament dimensions) plus a symmetric case that spotlights its `<tempo>` so the ornament dimensions are the *background* and `<temporalSpread frameLength="44.0">` demonstrably shrinks. Add one hand-built toneduration case. Then close the table with a guard asserting the union of the table's spared sets equals `EXPRESSION_DIMENSIONS`.

### [MINOR] "keeps every one of its attributes byte for byte" pins 3 of the element's 5 attributes

- **Claim:** The foreground pin is genuine byte/string equality on the serialized attribute text (not `toEqual` on parsed numbers), which is the right shape. But it asserts only `bpm`, `transition.to` and `meanTempoAt`; the spotlit element carries five attributes. The title states a completeness the assertions do not deliver, and `attributeAt` already extracts the whole start tag, so a strictly stronger whole-tag comparison is one line away.
- **Evidence:** `tests/api/spotlight.test.ts:165-170` asserts three attributes. The element in `tests/integration/fixtures/all-maps-reference/all_maps.mpm` is `<tempo date="2880.0" bpm="120" transition.to="90" beatLength="0.25" meanTempoAt="0.5" />` — `date` and `beatLength` are unpinned. (Risk is low: neither is a registry write site; `beatLength` is read-only normalization input, `registry.ts:892 TEMPO_BEAT_LENGTH_ATTRIBUTE`.) Same shape at `spotlight.test.ts:184-186` for the swapped-roles case and `:197` for `<movement>`.
- **Fix:** Have `attributeAt`'s sibling return the matched start tag and assert it equals the tag in `canonicalMpm(ALL_MAPS)` for the same id — one `toBe` that covers every attribute and its serialized spelling, making the title true.

### [MINOR] The five "unweighted" preset rows are pinned against the constant the preset is built from (tautology)

- **Claim:** Lens item 4 in the affirmative for the nine tuned numbers: they are pinned against **literal** values, not against anything the preset exports, and I verified them against the Java. The exception is the five-row block, which asserts `PROTOTYPE_WEIGHTS[d] === IDENTITY_WEIGHT` where `PROTOTYPE_WEIGHTS[d]` is *defined* as `IDENTITY_WEIGHT` — a tautology that cannot fail on a change to the constant.
- **Evidence:** `tests/expression/weights.test.ts:112-122` vs `src/expression/weights.ts:97-101`. Executed mutation: `IDENTITY_WEIGHT = 2` → the five-row block still passes (as does `carries all fifteen dimensions`); only the four `w = 1 passes s straight through` rows and `expect(factors.asynchrony).toBe(2)` fail, so the constant is transitively pinned but the correspondence rows are not. Java verification (`/Users/nielspfeffer/Projects/mpm-renderer/core/src/main/java/meicotools/core/PerformService.java:47-58`): tempo 1.0, dynamics 1.1, rubato 0.2, accentuation 1.3, temporalSpread 1.5, dynamicsGradient 0.3, relativeDuration 0.2, relativeVelocity 0.3 — all eight match, the 0.2 articulation collapse takes the lower as documented, and `ModifyService.java:85-94` confirms the exact spelling `(this.x - 1.0) * weights.x + 1.0`.
- **Fix:** Change line 121 to `expect(PROTOTYPE_WEIGHTS[dimension]).toBe(1);` — a literal, like the other nine rows — and keep `IDENTITY_WEIGHT === 1` as its own one-line assertion.

### [MINOR] Three smaller unpinned contracts: the `resolvedIds` defensive copy, most option-rejection messages, and a `Number(null)` vacuity window

- **Claim:** (a) `spotlightMpm`'s `resolvedIds` copy is annotated as CHARTER's public-API rule but is unpinned. (b) Of the ten option-rejection rows, only `attenuation: 0` asserts message content; the other six attenuation rows and all three `ids` rows assert the class alone, so neither the "required and must be a finite number in (0,1]" branch nor the "must be an array of xml:id strings" branch has its wording pinned. (c) `attributeAt` returns `null` on a miss and two assertions wrap it in `Number(...)`, where `Number(null) === 0` satisfies `toBeLessThan`.
- **Evidence:** (a) Executed mutation: replacing the `selection.resolved.map(...)` block in `src/api/expression.ts` with `resolvedIds: selection.resolved` → **3958/3958 passing**. (b) `tests/api/spotlight.test.ts:302-314` and `:320-328` are `toThrow(InvalidOptionError)` only; `:316-318` is the sole message assertion. (c) `tests/api/spotlight.test.ts:187` — `expect(Number(attributeAt(mpm, 'pickTempo', 'bpm'))).toBeLessThan(120)` passes if the attribute is absent. The paired line 188 (`toBeGreaterThan(90)`) does fail on `null`, so the case is half-guarded. Related doc nit: `src/expression/selection.ts:86-89` lists "a `<note>` id copied out of the score" as an `unmappable` example — an MPM carries no `<note>` elements, so such an id is `unresolved`; neither the claim nor a `<note>`-shaped case is tested.
- **Fix:** (a) Assert `result.resolvedIds[0].dimensions !== <the resolver's array>` or freeze-and-mutate. (b) Add `toThrow(/\(0,1\]/)` to the `>1`/negative rows and `toThrow(/array of xml:id strings/)` to the ids rows. (c) Make `attributeAt` throw on a miss instead of returning `null`, matching `withId`'s own no-silent-vacuity stance. Fix the `<note>` example in the docstring to say `unresolved`.
