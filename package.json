{
  "name": "meico",
  "version": "0.8.8",
  "description": "MEI Converter - TypeScript port of the Java meico library",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "verify": "npm run build && vitest run"
  },
  "dependencies": {
    "@xmldom/xmldom": "^0.9.8",
    "uuid": "^13.0.0",
    "xpath": "^0.0.34"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/uuid": "^10.0.0",
    "@vitest/coverage-v8": "^3.2.7",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
