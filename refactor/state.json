{
  "project": "meico-ts idiomatic refactor",
  "branch": "ts-idiomatic",
  "baselineCommit": "62c125f",
  "lastGreenCommit": "f5aef95",
  "phase": 4,
  "currentItem": "TD2",
  "counters": { "completed": 26, "blocked": 0, "reverted": 0 },
  "coverageBaseline": {
    "invariant": "v3 — see charter invariant 7",
    "functionsFloorPct": 92.0,
    "functionsFloorNote": "Rebased 2026-08-09, basis 92.3598 post-T15 dispatch restructure (+51 minted functions, accounting verifier-confirmed)",
    "phase2Start": {
      "coveredStatements": 13001,
      "totalStatements": 15294,
      "uncoveredStatements": 2293,
      "functionsPct": 94.0871,
      "testCount": 2108,
      "confirmed": "verifier-T3b reproduced all gated metrics bit-exactly on a scratch archive of 67b407e (2026-08-08); phase-2 uncovered budget = 2318"
    },
    "history": "86.69 stmts pre-format basis -> 85.03 post-prettier (units change) -> 85.0072 after T3+stub removal; ratio floor retired in favor of uncovered-count budget"
  },
  "queue": [
    {
      "id": "T1",
      "phase": 1,
      "status": "done",
      "title": "Add verify script + record baseline metrics",
      "details": "Add `verify` npm script (build then vitest run). Record baseline in log.md: test count, suite runtime, tsc runtime, scoped coverage numbers (npm run test:coverage). No src changes."
    },
    {
      "id": "T2",
      "phase": 1,
      "status": "done",
      "title": "Tooling baseline: ESLint (typescript-eslint strict+stylistic) + Prettier",
      "details": "Flat eslint config + prettier, npm run lint / lint:fix / format. Apply auto-fixes and a one-time prettier format as a PURE formatting commit (no logic). Document remaining (non-auto-fixable) violations per file in refactor/lint-debt.md as input for Phase 2 items. Style decisions to codify: no getX()/setX() Java accessors (prefer properties/accessors), prefer for..of & array methods, template literals, ===, no var, prefer const, explicit return types on exported API. ALSO (T1 verifier finding): tests are not typechecked by the build (tsconfig excludes *.test.ts) — close that hole, e.g. tsconfig.tests.json + extend the verify script with a no-emit typecheck over tests/, so type breakage in adapted tests surfaces at the gate, not just as vitest runtime failures."
    },
    {
      "id": "T3",
      "phase": 1,
      "status": "done",
      "title": "Excise out-of-scope modules",
      "details": "Scope = MEI / MSM+MPM => MIDI (see vitest.config.ts coverage include list). Remove via `git rm`: src/mei/Mei2MusicXmlConverter.ts, src/musicxml/**, src/audio/**, src/midi/Midi2MsmConverter.ts, src/supplementary/ColorCoding.ts IF unreferenced by in-scope code, and any other module unreachable from the in-scope surface (check imports transitively; also prune index.ts/Meico.ts exports and any dead tests for removed modules). Keep Midi playback-free: strip audio/playback methods from Midi.ts only if fully out of the dependency cone of MIDI file generation. Verify green."
    },
    {
      "id": "T4",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: supplementary/ (KeyValue, RandomNumberProvider)",
      "details": "Idiomatic TS within same public behavior. KeyValue likely becomes a readonly tuple or minimal class kept for API compat (full replacement is T16's call). RandomNumberProvider: field initializers, private #/private, remove Java-isms. Unit tests adapted."
    },
    {
      "id": "T5",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: xml/XomTypes.ts",
      "details": "Internal cleanup only — the XOM-emulation API surface stays (it is load-bearing for serialization equivalence). Idiomatic internals, doc comment explaining the layer's purpose and byte-compat constraints. Deeper redesign is T17."
    },
    {
      "id": "T6",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: mpm styles + defs",
      "details": "src/mpm/elements/styles/** — getters/setters to accessors/properties, constructor patterns, static factory cleanup, union types over pseudo-enums, remove dead Java idioms. Update unit tests mechanically."
    },
    {
      "id": "T7",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: mpm maps + data classes",
      "details": "src/mpm/elements/maps/** including data/. Same idiom checklist. Careful: rendering math must stay bit-identical; no reordering of floating-point operations."
    },
    {
      "id": "T8",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: mpm core (Mpm, Performance, Header, metadata)",
      "details": "Same checklist. Performance.perform flow untouched semantically (pipeline redesign is T19)."
    },
    {
      "id": "T9",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: msm/ cluster",
      "details": "Msm, AbstractMsm, Goto, etc. Same checklist."
    },
    {
      "id": "T10",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: mei/ cluster (Mei, Helper, Mei2MsmMpmConverter)",
      "details": "Local idioms only — no structural break-up yet (that is T14/T15). These are the biggest files; keep diffs reviewable by splitting into ≤3 sub-rounds if needed (still one commit)."
    },
    {
      "id": "T11",
      "phase": 2,
      "status": "done",
      "title": "Local idioms: midi/ cluster",
      "details": "Midi, MidiTypes, EventMaker, InstrumentsDictionary. InstrumentsDictionary's big data tables may become const data structures. Byte-identical MIDI output required (suite proves it)."
    },
    {
      "id": "T9b",
      "phase": 2,
      "status": "done",
      "title": "Fix Msm.getMinimalPPQ integer-division divergence from Java",
      "details": "Discovered in T9: getMinimalPPQ uses float division where Java Msm.java uses integer division (ppq 720, duration 22 -> Java 32, TS 1). Latent (zero src/ callers; unit tests only reach exact-division subdivisions). Fix TOWARD Java parity, add unit tests pinning the non-exact-division cases against hand-computed Java semantics (verify against the Java source, cite lines). Small item — one method + tests."
    },
    {
      "id": "T12",
      "phase": 3,
      "status": "done",
      "title": "Architecture design doc",
      "details": "Architect agent produces refactor/ARCHITECTURE.md: target module map, public API design (clean facade for MEI->MSM+MPM->MIDI), layering rules, null-vs-undefined policy, class-vs-function policy, error handling policy, what stays class-based for parity reasons, AND the immutability policy per the charter's 'immutable-friendly' section (where mutation is confined, what is readonly, immutable public API contract). Verifier reviews it for equivalence risk. No code changes."
    },
    {
      "id": "TD1",
      "phase": 3,
      "status": "done",
      "title": "Deliberate divergence: fix the articulateNote hang (parity ledger P3)",
      "details": "Conductor-approved deliberate divergence from Java (governance authority, journaled 2026-08-08; spec CORRECTED per verifier-T12 B2). ArticulationData.articulateNote's absoluteDurationChange branch loops forever (reproduced verbatim from Java, documented in T7). Fix: mirror ArticulationDef.articulateNote's spelling (ArticulationDef.ts:355-363, Java ArticulationDef.java:420-423) — BOTH the `if (dur > 0.0)` guard AND the `<=` comparison; the one-char `<=` alone does NOT terminate for duration<=0 with negative change (verifier-T12 simulated it; duration.perf=0.0 occurs in composite_advanced_augmented.msm). Requirements: (1) no fixture exercises the branch (verifier-T12 confirmed: zero absoluteDurationChange in fixtures) — still prove equivalence suite unaffected with pipeline probe; (2) pinning tests incl. the formerly-hanging positive-duration case AND the (duration<=0, negative change) case, both under timeout; (3) journal that the guard also suppresses the Helper.addToListAttribute(note,'modified',...) write on the guarded path; (4) site comment DELIBERATE DIVERGENCE #1 citing this item + both Java lines; (5) negative control: unfixed code hangs under timeout where fixed code terminates, for BOTH test cases. Small surgical item.",
      "order": "immediately after T12 verification, before T14"
    },
    {
      "id": "T19a",
      "phase": 3,
      "status": "done",
      "title": "Units brands + RenderOptions (ARCHITECTURE.md §7, §2)",
      "details": "Per ARCHITECTURE.md: src/units.ts (type-only brands: Ticks, Milliseconds, Normalized, Midi7Bit, Bpm — RULE U1-U3, zero emitted JS, proven by zero-line emitted-JS diff) and src/mpm/RenderOptions.ts (seed + sampling knobs incl. movementSampleMaxStep relocation from the mutable static, per the immutability policy). Runs after T18, before T13 (architect's order).",
      "order": "T14 -> T18 -> T19a -> T13 -> T16 -> T15 -> T17 -> T19 -> T20 -> T21 (architect §8, conductor-adopted)"
    },
    {
      "id": "T13",
      "phase": 3,
      "status": "done",
      "title": "Public facade + index exports",
      "details": "Implement the ARCHITECTURE.md facade (e.g. parseMei/convertToMsmMpm/perform/renderMidi) as additive API; index.ts exports curated; old entry points kept working. Integration tests may switch to facade only mechanically. ACCEPTANCE (charter, facade-plain-data bullet): facade outputs are plain JSON-serializable/structured-clone-safe data — survives postMessage, supports referential-equality memoization; XomTypes/XML types never appear in facade signatures. DOWNSTREAM CONSUMER REQUESTS (mpmify ML session, 2026-08-08): (a) batch path MSM+MPM-as-objects/JSON -> per-note {id, date, duration, milliseconds.date, milliseconds.date.end, velocity} + CC streams (movement/sub-note dynamics), no file I/O; (b) expose the imprecision RNG seed so synthetic datasets are reproducible."
    },
    {
      "id": "T14",
      "phase": 3,
      "status": "done",
      "title": "Split mei/Helper.ts into focused modules",
      "details": "Static-utility god class into cohesive modules per ARCHITECTURE.md. Pure moves + renames; call sites updated; no logic edits."
    },
    {
      "id": "T15",
      "phase": 3,
      "status": "done",
      "title": "Restructure Mei2MsmMpmConverter",
      "details": "Element-dispatch architecture (handler map per MEI element) per ARCHITECTURE.md, replacing the Java-style switch/instanceof cascades. Highest-risk item: sub-round per element group, verify after each."
    },
    {
      "id": "T16",
      "phase": 3,
      "status": "done",
      "title": "Model layer: composition & types (msm/mpm)",
      "details": "Per ARCHITECTURE.md: reduce inheritance depth, KeyValue->tuples if decided, typed attribute access helpers, GenericMap/GenericStyle generics cleaned, immutable-friendly model surface (readonly properties, no input mutation outside declared boundaries). Behavior-identical."
    },
    {
      "id": "T17",
      "phase": 3,
      "status": "done",
      "title": "XML layer isolation",
      "details": "XomTypes behind a slim internal interface with a clean TS API; serialization byte-compat proven by full suite. Rename to reflect role (e.g. src/xml/dom.ts). Only if ARCHITECTURE.md judges the risk worth it; otherwise document and close."
    },
    {
      "id": "T18",
      "phase": 3,
      "status": "done",
      "title": "Eliminate circular imports + enforce",
      "details": "Fix the Mpm/GenericStyle deep-import hazard and all other cycles (map with madge or eslint import/no-cycle); add lint enforcement so they cannot return."
    },
    {
      "id": "T19",
      "phase": 3,
      "status": "done",
      "title": "Performance rendering pipeline cleanup",
      "details": "Performance.perform + map-rendering flow into clearly composed stages per ARCHITECTURE.md. Floating-point operation order must not change. Ornamentation ms-domain rendering (Java OrnamentationMap.java:477-509 parity) is especially sensitive."
    },
    {
      "id": "T20",
      "phase": 3,
      "status": "done",
      "title": "MIDI generation layer cleanup",
      "details": "EventMaker/Midi restructure per ARCHITECTURE.md; event ordering and byte layout must stay identical (midi-byte-equivalence suite proves it)."
    },
    {
      "id": "T20b",
      "phase": 3,
      "status": "done",
      "title": "Mirror Java movement fixes + regenerate ground truth (USER-APPROVED 2026-08-08: 'yes, regenerate')",
      "priority": "run FIRST in Phase 3, before T12 — self-contained, no ARCHITECTURE.md dependency; updating ground truth early means every later item verifies against the corrected references",
      "details": "Java fork now has (mpmify session, 2026-08-08, MOVEMENT_FIX_TEST_PASS verified): (1) MovementData XML ctor reads `controller` as plain no-ns attribute into this.controller; (2) addMovement(MovementData) serializes controller (after protraction, before xml:id); (3) getMovementDataOf parses curvature/protraction/controller; (4) additive static MovementMap.movementSampleMaxStep=0.1 replacing the literal (default byte-identical); (5) GenerateAllMapsReference movement cases now use normalized 0..1 positions (regenerated all-maps movement fixtures will differ beyond the bug fixes — expect that in the diff). Mirror all five in TS, regenerate reference fixtures from the fixed fork (GenerateReference/GeneratePerformanceReference/GenerateAllMapsReference — commands in project memory meico-ts-proof-harness.md, mind the classpath-shadowing gotcha), verify suite green against new ground truth. HARD GATE: do not start until the user approves regenerating tests/integration/fixtures/** (invariant 2). Adapt mpmify's MovementFixTest as a TS unit test (controller preserved through round-trip; defaults-render differs)."
    },
    {
      "id": "T21",
      "phase": 4,
      "status": "done",
      "title": "Dead-code sweep + coverage audit",
      "details": "Remove now-dead code (ts-prune or knip), re-run scoped coverage, must be >= baseline floor. Update vitest.config.ts include list if module paths moved (mechanical only)."
    },
    {
      "id": "T22",
      "phase": 4,
      "status": "done",
      "title": "Package hardening + docs",
      "details": "package.json exports map, sideEffects, engines, declaration maps; README with API examples and the Java-parity story; document deliberate bug-for-bug parity list. ALSO: rename package to `espressivo` (npm name `meico` is taken; user adopted the new name — update package.json name + any self-references in README/docs)."
    },
    {
      "id": "TD2",
      "phase": 4,
      "status": "in_progress",
      "title": "Bug-fix wave: fix all obvious inherited bugs (user directive 2026-08-09)",
      "details": "Fix each as a TD1-discipline documented divergence, sub-round per bug with pipeline byte-probe between: (1) Mpm.isInNamespace two Java typos (Mpm.java:214,218) — accept the CORRECT spellings while KEEPING the misspelled ones accepted (compat with Java-written files; fixtures contain only currently-accepted names, so byte-probes must stay identical); INVERT the just-added T22 pinning test to assert both spellings accepted (journal the inversion). (2) AccentuationPatternDef.getAccentuationAt segment-end bug (Java ~:317; the dead countdown-loop guard) — fix to Java's evident intent; the istanbul dead-branch evidence says no fixture hits it; invert the pinning tests (AccentuationPatternDef.test.ts:284-336 area, Styles.test.ts:353). (3) Parity-ledger P1: malformed numeric attributes — match Java semantics (Double.parseDouble throws -> factory returns null -> def skipped) instead of keeping NaN-valued defs; implement per ARCHITECTURE.md §3/§6 error policy (internal: null-return skip path, NOT exceptions, unless the doc rules otherwise); fixtures are well-formed so probes stay identical; add malformed-input tests per def class. (4) P2: MovementMap.getPreviousPosition on missing transition.to — Java NPEs, TS silently yields 0: replace the silent 0 with the §6-policy error (typed error at parse or explicit skip per the doc's malformed-input ruling; journal the choice); add tests. (5) P4: RandomNumberProvider.getValue/getValueDouble on NaN/Infinity/huge indices — both Java and TS hang or overflow: add input guards (typed RangeError-class rejection), tests for each pathological class; this diverges from Java like TD1 (unusable-on-input beats hang). Each bug: PARITY.md 'Fixed bugs' entry with Java citations, site comment, negative control where feasible. What NOT to touch: ArticulationData overwrite-not-compose (ambiguous design intent — document as preserved), imprecision nondeterminism, anything not listed without a journaled conductor ruling. Full standard evidence duty; verify green; TREE FREEZE after READY."
    },
    {
      "id": "TD3",
      "phase": 4,
      "status": "pending",
      "title": "Segment-end accentuation fix + ground-truth regeneration (UNGATED: Java fix committed as meico@1d662105)",
      "javaFix": "meico@1d662105, AccentuationPatternDef.java:316-320 (verified: commit exists, 1 file 2+/2-). Semantics to mirror EXACTLY: `i >` -> `i <`, segmentEnd = next anchor's beat for non-last anchors; the LAST anchor's segment KEEPS patternLength+1 as its end (at beatPosition 2200 in the probe pattern: 40/721 = 0.05547850208044383 — denominator 721 not 720). Probe reference: mpmify/ml/java/AccentFixProbe.java (beat 1.0 -> 0.001388888888888889 -> velocity 100.00138888888888).",
      "details": "The TD2 wave's fix 2, deferred by conductor ruling (see [TD2] worker finding + conductor ruling in log.md): AccentuationPatternDef.getAccentuationAt segment-end bug IS exercised by the metrical_accentuation all-maps fixture, whose Java-generated reference pins the buggy velocity (100.0003471017008 vs fixed 100.00138888888888; delta 0.00104 hidden by the 0.01 all-maps tolerance). Plan (T20b pattern): (1) mpmify session applies + commits the one-character Java fix in the fork (requested 2026-08-09; ping expected with SHA); (2) TD3 worker mirrors the fix in TS exactly (cite final Java lines), regenerates the metrical_accentuation reference set (GenerateAllMapsReference; only accentuation-related files may change — categorize the diff), inverts the pinning tests with hand-computed FIXED values, PARITY.md Fixed-bugs entry updated from 'pending' to done; (3) evaluate tightening the all-maps numeric tolerance (DISCOVERED: 0.01 hides 1e-3 drift; port currently matches reference exactly, so tightening is safe — propose the value with evidence, needs verifier sign-off under invariant 3). Full TD1-discipline evidence."
    },
    {
      "id": "T23",
      "phase": 4,
      "status": "pending",
      "postCompletion": "USER DIRECTIVE 2026-08-09: after T23 PASS + final report, merge ts-idiomatic into main (--no-ff) and push both branches",
      "title": "Final adversarial audit",
      "details": "Fresh audit agent: full-diff review vs baseline commit for any semantic drift; perf smoke (suite runtime + a large-fixture render) vs baseline numbers from T1; verify all invariants held (fixtures untouched: git log on tests/integration/fixtures must be empty after baseline). Final report to user."
    }
  ]
}
